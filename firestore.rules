rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open read/write for a small private team.
    // To lock it down later, add Firebase Authentication and replace
    // `if true` with `if request.auth != null`.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
